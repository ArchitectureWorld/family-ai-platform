{
  "name": "family-ai-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@10.9.2",
  "engines": {
    "node": ">=22 <23"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:contracts": "npm run build -w @family-ai/contracts",
    "build:adapter-sdk": "npm run build:contracts && npm run build -w @family-ai/provider-adapter-sdk",
    "build:gateway": "npm run build:adapter-sdk && npm run build -w @family-ai/gateway",
    "build": "npm run build:gateway",
    "test": "npm run build:adapter-sdk && npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "check": "npm run test && npm run typecheck && npm run build",
    "start": "npm run start -w @family-ai/gateway"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
